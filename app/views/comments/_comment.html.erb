<li>
  <p><strong><%= comment.user.email %>:</strong></p>
  <p><%= comment.body %></p>
  <% if comment.user == current_user %>
    <%= link_to 'Delete', [comment.item, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
</li>
