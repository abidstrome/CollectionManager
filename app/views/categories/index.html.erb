<h1>categories</h1>
<% if current_user.admin? %>
    <%= link_to 'New Category', new_category_path, class: 'btm btn-primary'%>
<% end %>
<ul>
    <% @categories.each do |category| %>
        <li><%= link_to category.name, category_path(category)%></li>
    <% end %>
</ul>
    